import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

import {RevCreateNewStoreFormWidget} from './rev_widget_views/RevCreateNewStoreFormWidget';

export const RevCreateNewStoreForm = () => {
  return <RevCreateNewStoreFormWidget />;
};

const styles = StyleSheet.create({});
