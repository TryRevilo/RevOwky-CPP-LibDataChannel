import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const RevUserSettings = () => {
  return (
    <View>
      <Text>RevUserSettings</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
